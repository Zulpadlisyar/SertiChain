# RPC endpoint (e.g., Hardhat node) - default is http://127.0.0.1:8545
RPC_URL=http://127.0.0.1:8545

# If you deployed already, set the contract address (optional if using backend/last_deploy.json)
CONTRACT_ADDRESS=

# Server port
PORT=3000
